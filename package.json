{
  "name": "ai-patient-monitoring-root",
  "version": "1.0.0",
  "description": "Root package for deployment",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "setup-db": "node backend/scripts/init-db.js && node backend/scripts/update-db.js && node backend/scripts/create-notes-table.js && node backend/migrate.js && node backend/reset-admin.js",
    "build": "npm run install-all && npm run build --prefix frontend && npm run setup-db",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
